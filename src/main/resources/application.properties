spring.application.name=revision

# Redis configuration
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.database=0
spring.data.redis.username=
spring.data.redis.password=

#server.port=8080

# API Key for external use or internal service authentication\
# Pass-key for the API is set to "abc123"
my.api.pass-key=abc123

# Path to the CSV file that contains customer data
# The file path for the customer CSV file (relative to the project)
csv.file.path=./customers-100.csv